message("Building bindings for perl5: libdnf")


foreach(MODULE_NAME ${SWIG_LIBDNF_MODULES})
    add_perl5_module(libdnf ${MODULE_NAME})
endforeach()


set(LIB_CPACK_NAME "PERL_LIBDNF")
set("CPACK_DEBIAN_${LIB_CPACK_NAME}_PACKAGE_NAME" "${CPACK_DEBIAN_LIB_PACKAGE_NAME}-perl" PARENT_SCOPE)
set("CPACK_DEBIAN_${LIB_CPACK_NAME}_PACKAGE_DEPENDS" "libperl${PERL_VERSION_MAJOR}.${PERL_VERSION_MINOR} (>=${PERL_VERSION_STRING}~), python${Python_VERSION_MAJOR}.${Python_VERSION_MINOR} (>=${PERL_VERSION_STRING}~), ${CPACK_DEBIAN_LIB_PACKAGE_NAME}" PARENT_SCOPE)
