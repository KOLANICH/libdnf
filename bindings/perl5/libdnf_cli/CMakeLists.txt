if(NOT WITH_LIBDNF_CLI)
    return()
endif()


message("Building bindings for perl5: libdnf_cli")


foreach(MODULE_NAME ${SWIG_LIBDNF_CLI_MODULES})
    add_perl5_module(libdnf_cli ${MODULE_NAME})
endforeach()

set(LIB_CPACK_NAME "PERL_LIBDNF_CLI")
set("CPACK_DEBIAN_${LIB_CPACK_NAME}_PACKAGE_NAME" "${CPACK_DEBIAN_LIB_CLI_PACKAGE_NAME}-perl" PARENT_SCOPE)
set("CPACK_DEBIAN_${LIB_CPACK_NAME}_PACKAGE_DEPENDS" "libperl${PERL_VERSION_MAJOR}.${PERL_VERSION_MINOR} (>=${PERL_VERSION_STRING}~), perl${PERL_VERSION_MAJOR}.${PERL_VERSION_MINOR} (>=${PERL_VERSION_STRING}~), ${CPACK_DEBIAN_LIB_CLI_PACKAGE_NAME}" PARENT_SCOPE)
